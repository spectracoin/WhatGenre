<!DOCTYPE html>
<html>
  <head>
    <title>What genre?</title>
    <link rel="stylesheet" type="text/css" href="styles.css">
    <link rel="shortcut icon" href="favicon.ico">
    <link href="https://fonts.googleapis.com/css?family=Rubik:300,400,500" rel="stylesheet">
    <meta name="viewport" content="width=device-width, initial-scale=1">
  </head>
  <body>
    <nav>
      <a href="/"><div class="navbar-logo"><h1>| What genre?</h1></div></a>
    </nav>
    <section id="first-section" class="flex-container" style="background-image:linear-gradient(transparent,#000), linear-gradient(90deg,<%= @color1%>,<%= @color2%>);">
      <!-- first block div -->
      <div class="flex-item welcome-message">
        <h3>Start browsing</h3><h1>your music taste.</h1><h2>What's your favourite artist's genre?</h2>
      </div>
      <div class="flex-item search-block">
        <form action='/artist#artist' method='post'>
          <input name='artist' placeholder="Type artist's name"><button type='submit'>CLICK!</button>
        </form>
      </div>
    </section>
    <section id="artist" class="flex-container">
      <div class="top flex-container">
        <div class="flex-item">
          <%= if @check == true
            @genres.map { |genre| "<form action='/genre?genre=#{genre}&artist=#{@input}#third' method='post'><button type='submit'>#{genre}</button></form>"}.join("")
          end %>
        </div>
        <div class="flex-item">
          <h3>These are</h3>
          <h1><%=@input %>'s genres</h1><br>
          <!-- <h2>Click on a genre to find other artists.</h2> -->
        </div>
      </div>
      <div class="color1" style="background-image:linear-gradient(#000,transparent); background-color: <%= @color1 %>;">
        <div class="color2" style="background-image:linear-gradient(#000,transparent); background-color: <%= @color2 %>;"></div>
        <div class="image" style="background-image: url(<%= @image %>)"></div>
      </div>
    </section>
    <section id="third" class="flex-container">
      <div>
        <h3>These artists have the same genre as <%= @input %></h3>
        <%= @artists.map { |artist| "<button>#{artist}</button>"}.join("")%>
      </div>
    </section>
  </body>
</html>
